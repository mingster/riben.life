import { mingster } from "@/app/blog/authors";

export const meta = {
  title: "安裝 ddns (ddclient on Raspberry Pi)",
  description: "This is cheatsheet on ddclient toward google domains. ## 1. install ddclient ``` sudo apt-get install ddclient sudo apt-get install libio-socket-ssl-perl ```...",
  date: "2019-05-12T00:00:00.000Z",
  authors: [mingster],
};

This is cheatsheet on ddclient toward google domains.

## 1. install ddclient

```bash
sudo apt-get install ddclient
sudo apt-get install libio-socket-ssl-perl
```

## 2. config
after successful installation of ddclient edit the configuration file at /etc/ddclient.conf in your favorite editor

```bash
# Configuration file for ddclient generated by debconf #
# /etc/ddclient.conf
ssl=yes
#protocol=dyndns2
protocol=googledomains
use=web
server=domains.google.com
login=googledomaindyndnslogin password='autogeneratedpasswordformgoogle'
your.domain.com
daemon=100
```

```bash
sudo service ddclient restart
```

## DEBUG

```bash
sudo ddclient -debug -verbose -noquiet
```
