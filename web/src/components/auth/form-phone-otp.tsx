"use client";

// for user to enter phone number and send OTP
function FormPhoneOtp({ callbackUrl = "/" }: { callbackUrl?: string }) {
	return (
		<div>
			<h1>Phone OTP</h1>
		</div>
	);
}

export default FormPhoneOtp;
