import { LiffPhase0Status } from "./components/liff-phase0-status";

/**
 * Default LIFF entry for LINE Developers Endpoint URL smoke tests (`/liff`).
 */
export default function LiffBootstrapPage() {
	return <LiffPhase0Status />;
}
