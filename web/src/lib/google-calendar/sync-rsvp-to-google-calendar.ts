/**
 * Google Calendar RSVP sync is not wired in this schema/deployment.
 * riben.life uses Prisma models + googleapis; riben.life omits them — this is a no-op stub.
 */
export function queueRsvpGoogleCalendarSync(_rsvpId: string): void {
	// Intentionally empty
}
