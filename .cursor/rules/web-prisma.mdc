---
description: Prisma usage
globs: "**/prisma/**,**/lib/prismadb*"
alwaysApply: false
---

# Prisma

Schema under `web/prisma/`. Client via `@/lib/prismadb` (or project convention). Type-safe queries; transactions for multi-step; log errors with logger. Migrations: never edit applied migrations. See `prisma_v7.mdc` for upgrades.
