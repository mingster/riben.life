---
description: When to run builds
alwaysApply: false
---

# Builds

**Do not** run `bun run build` for routine edits.

Run when: Prisma/schema/tsconfig/deps change; structural route/import refactors; user asks; pre-release.

Skip for: copy, styles, single-file fixes, docs, rules.
