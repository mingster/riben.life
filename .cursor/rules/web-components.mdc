---
description: Local components only
globs: "web/src/**/*.{tsx,ts}"
alwaysApply: false
---

# Components

Import from `@/components/ui/*` and `@/components/*` only. **No** `mingster.backbone`. Shadcn in `components/ui`; feature components colocated or in `components/`. See `shared-packages.mdc`.
