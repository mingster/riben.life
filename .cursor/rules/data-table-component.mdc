---
description: DataTable for admin lists
globs: "**/columns.tsx,**/client-*.tsx"
alwaysApply: false
---

# DataTable

Use shared DataTable patterns with `columns.tsx`, row type in `*-column.ts`, filters in parent client component. Row actions via `cell-action.tsx` dropdown + confirm delete. Align with `CRUD-Guide.mdc` local state updates.
