---
description: Docs in /doc/
globs: "**/*.md,doc/**"
alwaysApply: false
---

# Documentation

One topic → one file in `/doc/` (`UPPER_SNAKE_CASE.md`). Prefixes: DEPLOYMENT-, MIGRATION-, REVIEW-, GUIDE-. AI rules stay in `.cursor/rules/`. Extend existing docs before creating duplicates.
