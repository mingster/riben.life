---
description: User agent skills
alwaysApply: true
---

# Agent skills

Skills live in `~/.agents/skills/<name>/SKILL.md`. Read that file when the task matches a skill (shadcn, stripe, tdd, etc.) before improvising. Repo may symlink via `web/.agents/skills-global` after link script.
