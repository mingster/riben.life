---
description: useSWR + hydration
globs: "**/client-*.tsx"
alwaysApply: false
---

# Client + SWR

`useIsHydrated()`; SWR key `null` until ready; Skeleton while loading; handle errors. Pattern: `const url = ready ? apiUrl : null; useSWR(url, fetcher)`.
