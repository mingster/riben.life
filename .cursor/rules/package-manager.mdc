---
description: Use bun only
globs: "**/package.json,**/bun.lock"
alwaysApply: true
---

# Package manager

**Bun only** — `bun install`, `bun add`, `bun run <script>`. Commit `bun.lock`. Remove `package-lock.json` / `yarn.lock` / `pnpm-lock.yaml` if present. Do not use npm/yarn/pnpm.
