---
description: Adding Cursor rules
alwaysApply: true
---

# Cursor rules

Only `.cursor/rules/<kebab-case>.mdc`. Frontmatter: `description`, `globs`, `alwaysApply`. Index: `.cursor/rules/README.md`. Prefer short always-on + glob-scoped detail.
