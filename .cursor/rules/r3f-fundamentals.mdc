---
description: React Three Fiber
globs: "**/*.{tsx,ts}"
alwaysApply: false
---

# R3F

Use `@react-three/fiber` + `@react-three/drei` patterns already in repo. Prefer existing scene/canvas components; dispose geometries/materials on unmount; limit re-renders (memoize heavy props). Match project camera/controls conventions in existing 3D files.
