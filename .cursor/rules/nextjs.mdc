---
description: Next.js App Router
globs: "web/src/app/**,web/src/actions/**"
alwaysApply: false
---

# Next.js

Await async APIs: `cookies()`, `headers()`, `params`, `searchParams` from page/layout props.

Server Components default; `"use client"` for browser state/events. Mutations via server actions.

See `web-app-routing.mdc` for storeAdmin layout rules.
