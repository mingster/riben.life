---
description: No mingster.backbone
alwaysApply: true
---

# Local components only

**Do not** import `mingster.backbone` — not in this repo.

Use `@/components/ui/*`, `@/components/*`, `@/lib/utils`, project hooks. All UI lives under `web/src/components/`.
